class roundSequence {
  constructor(gameJSON) {
    this.gameId = gameJSON.id
    this.rounds = []
    // this needs to create a round each time so it doesn't belong here
    // this is just for testing
    //this.createRound()
  }

  createRound() {
    //if (this.rounds.length) < 3 {
    //  this.rounds.push(new Round())
    //}
  }
}
